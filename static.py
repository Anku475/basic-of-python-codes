from functools import *
class ammount():
    @staticmethod
    def add(a,b):
        
        print(a+b)
ammount.add(5,6)

#we dont need of self in the static method . we use static method because we don't need of define object we can make object by the class name .

